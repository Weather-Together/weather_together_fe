<%= render partial: 'shared/nav' %>

<!DOCTYPE html>
<html lang="en">

<head>
  <style>
    body, html {
        height: 100%;
        margin: 0; /* Remove default margin */
        padding: 0; /* Remove default padding */
    }

    /* Set height for the top and bottom sections */
    .top-section, .bottom-section {
        height: 50%;
    }

    /* Set height for columns to take up 80% of the vertical space */
    .col-height {
        height: 80vh;
    }
  </style>
</head>

<body>
  <div class="container-fluid">
    <div class="row">
      <div class="col-md-6 d-flex flex-column col-height bottom-section">
        <div class="flex-fill">
          <h2>Today's Game</h2>
          <p>
            
          </p>
        </div>
      </div>

      <div class="col-md-6 d-flex flex-column col-height bottom-section">
        <div class="flex-fill">
          <h2>Personal Stats</h2>
          <p>
            <% @previous_rounds.each do |round| %>
              <%= round.close_date %><br>
              <%= round.target_weather_stats %><br>
              <%= round.status %><br>
            <% end %>
          </p>
        </div>
      </div>
    </div>
  </div>

</body>

</html>
